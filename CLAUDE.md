# まなびば プロジェクト概要（Claude Code用ガイド）

このファイルは、まなびば開発における方針・ルール・過去の経緯をまとめたものです。
Claude Codeはこのファイルを読み込んでから作業してください。

## プロジェクトの目的

中学1年生の娘（かりん）の家庭学習を支援するための、単一HTMLファイルの自己完結型Webアプリ。
サーバー不要、ブラウザのみで動作。GitHub Pagesで公開。

デプロイ先: `https://nachi-jazzmusic.github.io/manabiba/manabiba_v3.html`

## 開発の基本哲学（重要）

- アプリは「記録・スケジューリングのためのシステム」として使う。
  実際の勉強そのものは、紙の教材（ドリル・問題集）を使って行う。
  → アプリに問題を全部詰め込む方向ではなく、記録・管理・復習リマインドに徹する設計。
- 「できたつもり」（わかった気になっているだけで実は理解していない状態）を防ぐことを重視。
  そのための仕組みが「ルール型学習」（下記参照）。

## ルール型学習フレームワーク

- ドリル問題に「ルールキー」というタグを付与する。
- 生徒は答えを確認する前に、口頭でそのルールキーに対応する考え方を説明する必要がある。
- 対象教科：数学、英語文法、理科（生物・化学）、国語
- 対象外（暗記のみ）：歴史・地理、漢字
- ルールキーは事前に承認されたリストからのみ使用する（勝手に新しいキーを作らない）。
- カードのタグ表記：`ルール型｜key:キー名`

## フラッシュカードのデータ形式

### 現行フォーマット（line-break-separated形式）

各項目は改行で区切って1行ずつ記載する。

```
問題:
（ここに問題文）
選択肢:
（選択肢。複数ある場合は改行で区切る）
答え:
（正解）
解説:
（解説文）
```

- カード本文中に「｜」（半角パイプ）を使用しない（禁止）。
- 全角パイプ「｜」はタイトル行（デッキ名など）でのみ使用可。
- 旧フォーマット（`問題＝答え`形式、パイプ区切り）は廃止済み。混在させない。

### デッキの命名規則

```
[教科]｜[学年][学期]｜[種類]｜[単元]｜[テスト区分]
```

### その他の制約

- 1デッキあたり最大20問程度に収める。
- 手書きの書き込み（採点済みの丸バツなど）は無視して問題本文のみ抽出する。
- 画像がないと解けない問題には `【絵】` のフラグを付ける。
- べき乗はUnicodeの上付き文字を使う（例：5²）。

## 既知の課題・改善予定

- **英語スペルの自己採点精度の問題**：現状の自己申告方式では、スペルミスをスペルミスと認識できないまま「できた」と自己判定してしまうケースがある。
  → 対策：タイプ入力＋自動文字列マッチング方式に変更する。この変更にはHTML側の修正が必要（未着手）。

## Git操作について

Nachiはターミナルにコマンドを直接入力しない。Git操作はすべて会話での指示で行う。

- 「Pushして」「保存して」などと言われたら、以下を自動で実行する：
  ```
  git add .
  git commit -m "（変更内容がわかる一言メッセージを自動生成）"
  git push origin master
  git push origin master:main
  ```
- commitメッセージは、直前の変更内容をもとに自動で生成してよい（Nachiに聞き返さなくてよい）。
- 「Pushして」と言われるまでは、上記の標準フローの通りcommit・pushは行わない。
- pushの結果（成功/失敗）は必ず一言で報告する。エラーが出た場合は、対処法も含めて分かりやすく説明する。

## Git / GitHub運用ルール

- **作業ブランチは `master`。GitHub Pagesの公開元は `main`。** そのため「Pushして」の際は必ず両方に反映する（上記コマンド参照）。
  - 2026-08-22に発覚：mainが古いまま更新されておらず、masterにだけpushしていたため本番に変更が反映されない事故があった。以後、両ブランチへのpushを標準フローに組み込んでいる。
  - 将来的に運用をシンプルにする場合はmasterに一本化する選択肢もあるが、現時点ではブランチ整理は行わず「両方にpush」で運用する。
- **APIキーなど機密情報を含むファイルは絶対にコミットしない。**
  過去に `bk2/manabiba_backup_2026-08-11.json` にAPIキーが混入し、GitHubのpush protectionでブロックされた経緯がある。
  バックアップファイルをコミットする前に、APIキーが含まれていないか必ず確認すること。

## 実装時の標準フロー

機能追加・修正を依頼されたら、以下の順で進める。

1. 要件を実装する。
2. ブラウザで実際に動かして確認する（可能な場合、Claude in Chromeなどのブラウザ確認手段を使う）。
3. エラーや不具合があれば自分で直し、直るまで2に戻る。
4. 変更内容を、このファイルの「開発ログ」セクションに追記する（日付・変更点・変更理由を簡潔に）。
5. 下記フォーマットで「確認チェックリスト」を出力する。
6. ここで一度止まり、Nachiに完了報告する。**Gitへのcommit・pushは、Nachiの明示的な指示があるまで行わない。**

## 作業ルール（追加）

- 1つの改修が終わったら、次の改修は新しいセッション（New session）で始める。
  前の会話を持ち越さない（トークン節約のため）。
- 不具合の修正は最大3回まで。3回直らない、または同じエラーが2回続いたら、
  それ以上続けずに状況を報告して止まる。
- 仕様に書かれていない判断が必要になったら、推測で進めずに止まって質問する。
- 完了報告の最後に「指示に明記がなく自分で決めた点」を必ず列挙する。

## 完了報告時のチェックリスト出力フォーマット

実装が完了したら、以下の形式で「Nachiが目視確認すべき項目」を出力する。
一般論ではなく、**今回変更した箇所に紐づいた具体的な確認項目**にすること。

```
## 今回の変更内容
（何をどう変えたかを1〜3行で）

## 確認してほしいこと（Push前チェックリスト）
- [ ] （変更した機能が意図通り動くか。具体的な操作手順つきで）
- [ ] （既存の別機能を壊していないか。特に影響がありそうな箇所）
- [ ] （かりんが実際に使う想定での見え方・押しやすさ）
- [ ] （スマホ/タブレットなど、普段使っている端末での見え方）
- [ ] （エラーが起きた時の見え方・挙動）

## 変更したファイル
（ファイル名一覧）
```

チェックリストの項目数は変更の大きさに応じて増減してよいが、
「動いた」だけでなく「壊していないか」「かりんにとって使いやすいか」の観点は毎回必ず含めること。

## 開発ログ

（実装が完了するたびに、ここに日付・変更内容を追記していく）

- 【8月分・要約】ポメに聞く/親ポメの新規チャット化・チャット欄拡大・音声/手書き入力追加。すきま時間の曜日ローテーション導入と小テスト対策の重複解消。対策プランの類題ミスマッチ修正(`ruleId`集計方式に変更)。「今日のメニュー」のステップ表示を①〜⑤の詳細構造から教材名+ページ+類題ボタンの2〜3行へ簡素化。スマホでのJSON一括誤答登録機能を追加(`commitPaperEntry()`/`classifyMistakeTag()`に共通化)。復習テストが正解しても消えない不具合を2回対処(`renderRev()`呼び出し順序)。一括取り込みの連打による重複登録が発生し、進捗表示追加＋重複整理ボタンで対処。詳細はGit履歴（2026-08-22〜08-31のコミット）を参照。
- 2026-09-01：データ管理画面を整理。
  - 「🩹 うまく動かないときの修理」カード(黄色い枠)の4ボタン(何が容量を使っているか調べる/カードの中身を点検する/画像を専用の保存場所へ移す/重複しているデッキを1つにまとめる)を削除。中身のうち「使われていない画像を消す」「誤答記録の重複を整理する」の2つはバックアップカードへ移設(空にならなかったため見出しごとの削除はせず)。
  - 「📷 写真つきカードを1枚追加」フォームを削除。
  - 削除前に使用箇所を確認し、`diagnoseStorage()`/`cleanOtherKeys()`はバックアップ復元失敗時のエラー処理から今も呼ばれているため関数は残し、ボタンだけ削除(`#diagResult`の表示先も残置)。`checkCards()`/`renderCheck()`/`saveCheck()`/`fixTrailing()`/`rescueEmbeddedImages()`/`dedupeDecks()`/`pastePhoto()`/`addPhotoCard()`は他から呼ばれていないことを確認して関数ごと削除。`compressImage()`/`storeImage()`/`idbPutNew()`等の共通関数は他画面でも使われているため残置。
  - 削除したボタン名を文中で参照していたエラーメッセージ2箇所も、実情に合わせて文言を修正。
  - 既存データ(保存済みのデッキ・カード)への影響はなし。
- 2026-09-01：「今日の入力」で、教科書を忘れて今日のページが分からない時の補助機能を追加。
  - 各教科のページ入力欄の横に「❓わからない時はこちら」ボタンを追加。押すと`UNITMAP`(既存の教科書単元目次データ、6教科すべて完備)を単元名つきで一覧表示するモーダル(`openUnitPicker()`)が開き、選ぶとその単元の終了ページをそのまま入力欄に入れる(`pickUnitPage()`)。
  - 既存の手入力方式はそのまま残し、選択後も数字を直せる(置き換えではなく補助)。
  - 動画は個別の単元対応データが無い(`S.rules`の`vid`は一部のルールにしか無く、国語・歴史には皆無)ため、教科ごとのやさまる再生リストへのリンク(`plURL()`、既存関数)を補助的に添えるだけにとどめた。
  - `PDRILL`(市販ドリルの目次)はページ番号が教科書と別物のため、今回は使用しない(検討の結果、除外)。
  - `unitOf()`/`unitRangeOf()`/`unitRangeByCh()`/`pickSubjectsForToday()`/`makeMenu()`等、ページ番号を軸にした既存ロジックは無変更。書き込むデータ形は手入力と同じ`{page:Number}`のため、他画面への影響なし。
- 2026-09-02：対策プラン項目に、フル入力フォームを使わないワンクリック解消ボタンを追加。
  - 「今日のメニュー」の対策プラン項目に「✅ もう間違えなくなった」ボタンを追加(`resolvePlanItem()`)。押すと`commitPaperEntry()`で`{sub, tag, result:"green", where:"対策プランから解決登録", q/a/note:空}`という最小限のentryをS.paperLogに1件追加する。
  - 既存の`isTagResolved()`(同じ(教科,タグ)の中で一番新しい記録がgreenか)の仕組みにそのまま乗るが、同日内の記録が複数あると並び順に依存する既知の弱さがあるため、確実性のため`item.done=true`も直接セットして即座に「済み」表示にする。
  - ボタンの下に「本当に完璧に解けるようになった時だけ押してください」の注意書きを表示。確認ダイアログは追加せず(依頼通り)。
  - 誤答の登録画面のフル入力フォーム・「まとめて『できた』を記録」欄は変更なし。
- 2026-09-07：「参考情報」画面に、歴史向けの新しいYouTubeチャンネル2件を追加。
  - 「参考情報」画面は`S.extLinks`を`kind==="ref"`で絞り込んで表示しているだけで、UNITMAP(単元目次)との紐付けは元々存在しないこと、`kind:"ref"`は既存の1回きりの移行ブロック(`S.extLinksV7`〜`V12`)で付与されていることを確認。同じパターンで`S.extLinksV13`ブロックを新設し、「ぴよぴーよ速報」「いかを」を`subs:["his"]`(歴史のみ、地理は別途検討)で追加。
  - リンク先が個別動画ではなくチャンネルの動画一覧ページのため、単元と機能的に連動する遷移は作らず、代わりに軽量な`note`(目安となる時代・単元のテキスト)フィールドを新設し、`renderRef()`のカード表示にそのまま出すだけにとどめた。
  - `VIDEO_PL`/`plURL()`・`S.rules`の`vid`とは無関係な別データ。歴史(his)のS.rulesは引き続き0件、地理(geo)はg1/g2のみでvid無し、と確認(変化なし)。
- 2026-09-07：不具合修正。復習テストで、1〜2問目は正解して消えるが3問目あたりから消えなくなる。
  - 原因(2026-08-25の不具合とは別物と確認)：正解の1.6秒後に`renderRev()`が一覧を丸ごと作り直す仕様のため、その間に別の問題へ入力しかけていた内容が空欄にリセットされてしまい、そのまま「こたえあわせ」を押しても`judgeRev()`の空欄ガード(`if(!v)return;`)で静かに何も起きなくなっていた。今日のメニュー経由/類題チャレンジから直接、どちらでも起きる(画面遷移経路は無関係)。UNITMAPの単元ピッカー・対策プランの解消ボタンは`S.reviewQ`/`renderRev()`に一切触れておらず無関係と確認。
  - 修正：`renderRev()`が一覧を作り直す前に、各問題の入力中の内容を保存し、作り直した後に同じ問題(id)へ復元するようにした。Node.jsでの簡易シミュレーションで、修正前は入力内容が消え(再現)、修正後は保持される(解消)ことを確認ずみ。
- 2026-09-11：参考情報・デジタル教材カードの見た目調整と、デジタルドリルの画面分割。
  - 【参考情報カードの見た目】`.extlink-row`をflexからCSS gridに変更し、幅を統一。アイコン表示用の`.extlink-ic`(56×56固定、画像と同サイズ)を新設し、サムネイル有無で左側の幅が揃うようにした。タイトル(`.t`)は1行、説明文(`.s`)は2行までのline-clampで高さを固定し、長文でもレイアウトが崩れないようにした。
  - 【デジタルドリルの分割】従来`sCards`(暗記カード)画面の`#extLinkCards`に埋め込まれていたデジタル教材一覧(メクリカ・さくさくスタディ・タイピング等、`S.extLinks`の`kind!=="ref"`のもの)を、独立した新画面`sDrill`(サイドメニュー「デジタルドリル」)に移設。参考情報画面(`renderRef()`)と同じ「教科タブ+カード一覧」の型をそのまま流用し`renderDrill()`として新設。
  - サイドメニューの「暗記カード・デジタルドリル」は「暗記カード」に改称。`renderExtLinks()`からは`#extLinkCards`向けの分岐を削除(設定画面の`#extLinkList`向けの分岐は変更なし)。`renderCards()`からの`renderExtLinks()`呼び出しと、不要になった`extLinkExpanded`state変数も削除。
  - すきま時間ローテーションは各デジタル教材へ直接`window.open()`しており、この画面を経由しない設計のため変更不要と確認。`S.extLinks`のデータ自体・進捗には一切手を加えていない。
- 2026-09-12：新機能「学習マップ」（S.rules 22件をヒートマップ表示する画面）を追加。表示専用・既存データは読み取るだけで書き込みなし。
  - `ruleHeatLevel(r)`で1〜4段階を算出。gen持ちルールは既存の`status`(none/passed/mastered)をそのまま使用。gen:nullの10件(m1_4,m1_6,m1_8,m1_9,m2_2,m2_3,m2_4,s1,g1,g2)は類題チャレンジの検証フローに乗れず`status`が常に"none"のままという制約があるため、代わりに`S.paperLog`でそのルールIDに紐づく最新記録(green=できてきた/それ以外=取り組み中)で代用。この10件は「定着(◎)」に到達しない仕様とし、マスに「※」の注記＋画面上部の凡例で明記した。
  - `ruleHeatWarn(r)`で「怪しいかも」の⚠️を表示。`ctxSummary()`と同じ直近14日以内の`tagHist`、または期限切れ(未消化)の`S.reviewQ`のいずれかがあれば警告。
  - 教科ごとにセクションを分け、各セクション内は`pr`(優先度)昇順で並べる`.heat-grid`を新設。タップすると詳細モーダル(状態・注記・直近の誤答傾向)を表示し、gen持ちルールのみ「✏️ 類題チャレンジへ」ボタンで`startRule()`にジャンプできる(gen:nullは元々`startRule()`側でトースト表示に留まる仕様のためボタン自体を出さない)。
  - サイドメニュー「記録・ごほうび」グループに追加。Node.jsでレベル判定・警告判定のロジックを単体テストし、全ケースで想定通りの結果を確認ずみ。
- 2026-09-13：学習マップを、S.rulesだけを並べる形からUNITMAP(教科書の単元目次)を土台にした階層構造へ作り直し。
  - 検証の結果、既存の`chapterMatch()`/`unitRangeByCh()`(章名のあいまい一致)では22件中9件(数学1章)しか正しく紐付かないと判明。数学2章は単元が細かく分かれておりrule.chが粗すぎ、英語・理科・社会は表記ゆれで不一致になっていた。
  - 対応として、`INIT_RULES`の各項目に`unit`(UNITMAPの単元名と完全一致させる手動指定)を追加。17件は単元名から特定(うち英語3件はUNITMAPのUnit表記に内容説明が無いためNEW HORIZON1年の一般的構成からの推測、他はUNITMAPのページ範囲・単元名から確認)。数学0章(m0_1〜m0_5)はUNITMAPに対応する単元が存在しないため`unit`を設定せず「ひも付けなしルール」として教科末尾に表示。Node.jsで全22件のunit値が実際のUNITMAP行と一致することを検証ずみ。
  - `renderHeatmap()`を作り直し、外側のタイルはUNITMAPの単元(数学12/英語11/理科11/地理19/歴史15/国語7=計75単元)、タップすると中の子ルールが展開される階層表示に変更(`unitsWithRules()`/`openUnitDetail()`新設)。
  - 単元タイルの色は中の子ルールの最小レベル(`unitHeatLevel()`)、警告は子ルールのいずれかにあればOR(`unitHeatWarn()`)で集約。ルールが1件も無い単元は新設のレベル0(記録なし・破線グレー)で区別。
  - 色を緑系の単色グラデーション(薄いグレー→薄緑→中間緑→濃い緑)に統一。警告(⚠️)は引き続きオレンジ寄りの別系統のまま。
  - ルール単体の詳細モーダルに「← 単元にもどる」の導線を追加(`openHeatDetail()`に`backTo`引数を追加)。
- 2026-09-14：不具合修正。「単元にひも付けられなかったルール」欄に、数学16/18件・英語3/3件が入ってしまっていた。
  - 原因：`S.rules`は`localStorage`の既存保存データを読み込む仕組みのため、`INIT_RULES`に`unit`を追加しただけでは既存データには反映されない。新フィールド追加時は既存データへの移行処理も必要という、これまで守ってきたルールを`unit`追加時だけ入れ忘れていた(確信度◎○の数学1章・m2_1/m2_4・s1まで外れて見えたのはこのため)。
  - 修正：`load()`に、`S.rules`の各ルールへ`INIT_RULES`から`unit`を毎回上書きする移行処理を追加。`unit`は香凜さんの学習記録ではなく開発側のキュレーションデータのため、既存の`status`/`tagHist`等には触れず`unit`だけを常に最新の対応表で同期する。
  - 検証：「unitフィールドが無い旧保存データ」をNode.js上で再現し、修正後のロジックを適用した結果、数学13/18件(0章5件は設計通り未紐付け)・英語3/3件・理科1/1件・地理2/2件が正しく紐付くことを確認ずみ。
- 2026-09-19：学習マップに、中間テスト(10/9・10/13)向けの「テスト範囲登録・カウントダウン」機能を追加。
  - 新state `S.examRange`(配列、`{sub,date,pStart,pEnd}`)を新設。既存の`S.tests`(点数記録・「テスト結果」画面用)とは無関係な別データ。`load()`に`if(!S.examRange)S.examRange=[];`の移行処理を追加。
  - ページ範囲→単元の自動特定は、既存の`unitOf()`/`unitRangeOf()`(1点のページ→単元)をそのまま転用せず、UNITMAPを範囲重なりでフィルタする新関数`unitsInPageRange(sub,pStart,pEnd)`を新設。`unitsWithRules()`内で使う`unitInExamRange(sub,start,end)`も新設し、各単元オブジェクトに`inExam`を持たせた(既存の`unitHeatLevel`/`unitHeatWarn`のロジックには触れていない)。
  - 学習マップ画面(`#sHeatmap`)の凡例カードの下に「📅 テスト範囲」カード(`renderExamCard()`)を新設。カウントダウン表示・登録一覧は誰でも見られ、登録フォーム(教科・テスト日・ページ範囲)と削除ボタンは`S.parent`(保護者モード)時のみ表示(`renderTests()`の既存パターンを踏襲)。過ぎたテストはカウントダウンから外れ、一覧には「終了」表示で残す(削除は手動)。
  - 単元タイルには、テスト範囲内かどうかを示す別軸として`.examing`(枠線)+📅バッジを追加。既存の色分け(未着手〜定着)・「記録なし」の色はユーザー要望通り無変更。「テスト範囲のみ表示」トグル(`toggleExamFilter()`)を追加し、ONの間はテスト範囲が1件も無い教科セクションごと非表示、範囲外の単元タイルのみ非表示にする。
  - 「今日のメニュー」「誤答の登録」「対策プラン」「復習テスト」など他画面のロジック・データは未読み書きで無関係。既存データへの影響なし。
  - Node.js上に簡易DOMスタブを組み、登録→カウントダウン表示→バリデーション(不正な範囲・未入力の日付を拒否)→フィルタON/OFF→過去日の「終了」表示→削除、の一連の流れを再現して想定通り動くことを確認ずみ(実機ブラウザでの目視確認は未実施、Nachiに依頼)。
- 2026-09-19(続き)：上記「テスト範囲」カードのUI改善(Nachiからのフィードバックで2点)。
  - 縦幅の圧縮：登録フォームを`pf-row`(項目ごとにラベル+入力を縦積み)から、教科セレクト・日付・開始〜終了ページ・追加ボタンを横1行に並べる`.examaddrow`(flex-wrap)に変更。3段以上あった入力欄が1〜2段に収まるようにした。
  - 修正機能の追加：一覧の各行を、保護者モード時は表示専用のspanではなくその場で直接書き換えられる入力欄(教科セレクト・日付・開始/終了ページ)に変更(`updateExamRange(i,field,value)`新設)。`onchange`(値確定時)で保存・再描画する方式にした。以前の不具合(2026-09-07、入力中に画面を作り直して入力内容が消えた件)を踏まえ、入力の度に再描画される`oninput`ではなく、確定時にのみ発火する`onchange`を選んでいる。
  - 実装のついでに気づいた不具合を修正：`unitInExamRange()`が日付を見ていなかったため、テスト日を過ぎたテスト範囲(一覧では「終了」表示になっているもの)が単元タイルの📅バッジ・「テスト範囲のみ表示」フィルタにいつまでも残ってしまっていた。`x.date>=today()`の条件を追加し、過ぎたテストは地図上のハイライトからも外れるようにした。
  - 誤って`.pf-row input[type=number]`に付けていた汎用CSS(前回実装時に追加)を削除。「誤答の登録」画面の別フォーム(`#pq_n`)に意図せず見た目の変更が及んでいたため、影響範囲をテスト範囲カード専用のクラス(`.examaddrow`/`.examedit`)に閉じ直した。
  - Node.jsの簡易DOMスタブに編集シナリオ(既存エントリのページ範囲・教科を書き換え→タイルへの反映、不正な空値は既存値を保持)を追加して確認ずみ。
- 2026-09-19(続き2)：「テスト範囲」カードの登録一覧・入力フォームを折りたたみ式に変更(Nachiからの依頼:入力し終えたら常時表示は不要なため)。
  - 「あと◯日」のカウントダウンは常に表示したまま、その下の登録一覧・入力フォームだけを`▼ 登録・修正する`/`▲ とじる`ボタンで開閉できるようにした(`toggleExamCardOpen()`新設)。既定値は閉(`let examCardOpen=false`)。
  - 開閉状態は`examFilterOn`(テスト範囲のみ表示フィルタ)と同じく画面用の一時変数で保持し、`S`には保存しない(画面を離れたらリセットでよい)。登録・修正・削除のたびに`renderHeatmap()`が呼ばれてカード全体を作り直す既存の仕組みのままだが、この変数のおかげで、編集の途中でカードが勝手に閉じることはない(Node.js上で「編集操作をはさんでも開いたまま」を確認ずみ)。
- 2026-09-19(続き3)：不具合修正。`updateExamRange()`(一覧編集)で開始ページ>終了ページになる修正をすると、警告トーストは出るのに保存はされてしまい、`addExamRange()`(新規登録)側の拒否動作と非対称になっていた(Nachiからの指摘)。
  - 原因：`x[field]=n`で値を書き込んだ後に`if(x.pStart>x.pEnd)toast(...)`と警告だけ出していたため、警告後も不正な値がそのまま`S.examRange`に残っていた。
  - 修正：`addExamRange()`と同じ考え方で、書き込む前にpStart/pEndの一方だけを仮に置き換えた値で大小関係を判定し、逆転する場合はトーストを出して`x[field]`への代入自体を行わずに`return`する(画面は`renderHeatmap()`で再描画され、入力欄の表示も保存済みの元の値に戻る)。
  - 検証：Node.js上のシミュレーションに、pStart側・pEnd側それぞれから逆転させようとするケースを追加し、どちらも値が変わらず元のまま保たれることを確認ずみ。
- 2026-09-25：学習マップ・テスト範囲機能を拡張し、「テスト勉強モード」を新設。中間・期末テストのたびに使う恒常機能として、テスト範囲内の単元を定着レベル順に優先度づけし、「今日のメニュー」のメイン学習を自動的にテスト勉強用に切り替える。
  - 新state `S.examMode`(boolean)・`S.examPlan`(`{createdAt, units:[{sub,unit,level,target,done}]}`)を新設。`load()`に移行処理を追加。`S.examRange`(テスト範囲登録)・`S.plan`(対策プラン)とは別の独立データ。
  - 対象単元は、既存の`unitsWithRules()`が持つ`inExam`(過去日を除いたテスト範囲判定、2026-09-19実装ずみ)をそのまま使い、教科をまたいで1本のリストにする(`buildExamPlanUnits()`新設)。優先度は既存の`unitHeatLevel()`(0〜4)をそのまま流用。
  - 目標消化回数(target)はレベルから決め打ち：レベル0〜2は2回、レベル3〜4は1回(`EXAM_TARGET_BY_LEVEL`、Nachiと相談のうえレベル2は2回に簡略化)。日ごとの固定カレンダーは作らず、「単元ごとの残りノルマ(target-done)」だけを持たせ、`pickExamUnitsForToday()`が毎日その場でレベルの低い順に未消化の単元を最大2件選ぶ動的方式にした(対策プランの`topPlanItem()`と同じ考え方。1日サボっても翌日以降の予定がズレない)。
  - 目標(target)の再計算は自動では行わず、`🔄 計画を作り直す`ボタン(`rebuildExamPlan()`)を押した時のみ。実績(done)は「終わった!」を押すたびに自動で増える(`markBlock()`に追記)。作り直し時は同じ(教科,単元)の`done`を引き継ぐため、進捗は失われない。既存の対策プラン`isTagResolved()`が「なぜ変わったか分からない」という既知課題(本ファイル記載)を踏まえ、目標が誤答登録のたびに裏で動かないようにする設計判断。
  - 「今日のメニュー」への差し込みは、`renderMenu()`内の`const picks=pickSubjectsForToday();`を`(S.examMode&&S.examPlan)?pickExamUnitsForToday():pickSubjectsForToday()`という1行の分岐に変更しただけ。`pickExamUnitsForToday()`は既存の`kind:"today"`ピックと全く同じ形(`{kind,sub,label,why,rule,unit,page}`)のオブジェクトを返すため、`stepFlowHtml()`・`swapPick()`は無改造。`page`に単元の開始ページ(`unitStartPage()`新設)を入れることで、`stepFlowHtml()`内の`unitRangeOf()`がそのまま狙った単元の教科書案内を出す。
  - `recordMainPicks()`に`S.lastExamUnits`(既存の`S.lastPlanIds`と同じパターン)を1行追加し、`markBlock()`に対策プラン由来の項目と同じ形の分岐を1ブロック追加(該当単元の`done`をtargetを超えない範囲で+1)。`pickSubjectsForToday()`・`makeMenu()`本体・対策プラン・誤答の登録・復習テスト・暗記カード・すきま時間ローテーションは無改造で影響なし。
  - UIは「📅 テスト範囲」カードの、折りたたみの外(常に見える位置)にモード切り替えボタン・作り直しボタン・残りタスク数のサマリー(`examModeCardHtml()`/`examModeSummary()`)を追加。「今日のメニュー」の対策プランバッジの隣にも、モードON中は軽量なバッジを1つ追加(`#menuPlanBadge`を共用、HTML変更なし)。
  - Node.js上のシミュレーションで、範囲未登録時の起動拒否、レベル→target割り当て、優先順位(レベル低い順)での選定、「終わった!」時に該当単元だけ`done`が進むこと、targetの上限、作り直し時の`done`引き継ぎ、モードOFF後も`S.examPlan`が消えないことを確認ずみ(実機ブラウザでの目視確認は未実施、Nachiに依頼)。
- 2026-09-25(続き)：テスト勉強モードのUI改善3点(Nachiからのフィードバック)。
  - 【残タスクの内訳】`examModeSummary()`(件数のみ)を`examModeSummaryHtml()`に置き換え、教科ごとに残っている単元名を`、`区切りで一覧表示するよう変更(例:「英語：Unit0、Unit1」)。学習マップの「📅 テスト範囲」カードに加え、「今日のメニュー」側にもモードON中は同じ内訳を表示する専用カードを追加(`renderMenu()`のメイン学習欄の直前に挿入)。`#menuPlanBadge`に付けていた簡易バッジ(件数のみ)は、この専用カードに一本化するため削除。
  - 【サイドメニューの位置】「学習マップ」ボタンを「記録・ごほうび」グループ(◎図鑑の下)から、「毎日のはじめに」グループ(今日の入力・今日のメニューと同列、末尾)へ移動。`go()`は`data-s`属性で画面を判定する作りのため、ボタンの位置を変えても動作に影響なし。
  - 【登録完了の分かりやすさ】テスト範囲の登録ボタンの文言を「＋ 追加」から「✅ この範囲を登録する」に変更。あわせて、追加した直後の行だけ一瞬ハイライトする`.examadded`(CSSアニメーション`examFlash`、1.6秒でmint色→透明にフェード)を新設。`let justAddedExam`(直前に追加したS.examRangeの要素への参照、保存しない)で「どの行か」を覚えておき、`renderExamCard()`が1回描画したら即座にnullへ戻すことで、次に別の理由で再描画されてもハイライトが残らないようにした。
  - Node.js上のシミュレーションに、内訳HTMLに単元名(完了済みは含まれないこと含む)が正しく出ること、追加直後は`examadded`クラスが付き、次の描画では消えることを検証するテストを追加して確認ずみ。
- 2026-09-26：学習マップに、ルールが1件も紐づいていない単元(全75単元中68単元、前回の調査で判明)向けの「手動理解度チェック」機能を追加。
  - 新state `S.manualPatterns`(フラット配列、`{id,sub,unit,name,stage(0〜3),warn}`)を新設。`stage`は0=未実施/1=やった/2=わかった/3=説明できる(既存の定着レベル4=◎には手動入力では到達しない上限)。`warn`は段階とは独立の苦手フラグ。`load()`に初期化と、初回だけ既定の参考パターン例を投入する`SEED_MANUAL_PATTERNS`(`S.manualPatternsSeeded`で1回だけ実行、以後は完全にユーザーが自由に編集・削除できる既存の`S.extLinksV13`等と同じ移行パターンを踏襲)を追加。
  - シード内容:数学「3章 方程式」5件・「5章 平面図形」3件(単元名が一意で迷いなし)、理科「単元1-1〜1-3章」に観察手順/植物/動物の分類3件(内容と単元名が対応)、英語Unit3〜7に文法トピックを1つずつ5件、歴史「1章 歴史へのとびら」に通史的な視点3件。**英語Unit3〜7への割り当てと歴史の代表単元選定は教科書目次を確認していない推測**(既存の英語Unit2のルール推測コメントと同じ扱い)。Nachi了承のうえ、実装後に確認・修正してもらう前提で進めた。
  - `unitsWithRules()`に1行追加し、各単元へ`patterns`(sub+unit完全一致の`S.manualPatterns`)を付与。`unitHeatLevel()`/`unitHeatWarn()`を「ルールが1件でもあればルールのみを見る、無ければパターンのみを見る」という完全排他の分岐に変更(`ruleHeatLevel()`がgen有無で類題検証/paperLog代用を切り替えているのと同じ「二重の仕組みにしない」思想)。ルールが1件以上ある7単元は無改造のまま。
  - `renderHeatmap()`の単元タイルは、ルール0件・パターンありの単元にも既存の「※(自己申告のみ・◎に到達しない)」注記を表示するよう分岐を追加し、下部ラベルも「◯件のパターン」に変更。凡例の説明文も「手動で入力した単元も同様」に一言追記。
  - `openUnitDetail()`のルール0件時の中身を、「記録がありません」の案内だけから、既存の`renderExamCard()`(一覧+追加行)と同じ型のパターン管理UI(`patternListHtml()`新設)に置き換え。段階選択は既存の`.lvsel`/`.lv`/`.lv.on`ピル型セレクタ(元々別用途で1箇所使われていたスタイルを再利用)、苦手フラグ・削除は`.mini`ボタン。`addManualPattern()`/`setPatternStage()`/`deletePattern()`/`togglePatternWarn()`はいずれも操作後に`renderHeatmap()`(タイルの色に反映)と`openUnitDetail()`(モーダル内の表示に反映)を呼び直す。
  - 編集権限：`S.examRange`(保護者モード限定)とは異なり、この機能は香凜さん自身がその場で自己申告する性質のもの(誤答登録の自己判定と同種)のため、保護者モードに関わらず誰でも操作できる(Nachi了承ずみ)。
  - テスト勉強モードとの連動：`buildExamPlanUnits()`は`unitHeatLevel()`の返り値をそのまま使うだけの作りだったため、**無改造**でパターンベースの単元もtarget/レベル計算の対象になった。
  - Node.js上のシミュレーションで、シード件数(19件)、パターンの単元紐付け、排他性(ルールがある単元はパターンを完全に無視する・その逆も)、記録が何も無い単元は従来通りlevel0のままであること、追加・段階変更・苦手フラグ・削除の一連のUI操作、そして`buildExamPlanUnits()`がパターンベースの単元を無改造で正しく拾う(level→target変換含む)ことを確認ずみ(実機ブラウザでの目視確認は未実施、Nachiに依頼)。
- 2026-09-26(続き)：単元詳細モーダルのUI改修4点(Nachiからのフィードバック)。
  - 【ルール表示の素っ気なさ】`openUnitDetail()`のルールあり分岐(`u.rules.length>0`)の先頭に「この単元は自動で記録される項目です。誤答の登録・類題チャレンジの結果がそのまま反映されます。」を追加し、`patternListHtml()`側の常時表示の説明文とトーンを揃えた。
  - 【title属性→常時表示】ルールタイルの「※」(`!r.gen`、「◎に到達しない」の意味)に付けていた`title`属性のツールチップを廃止(タッチ操作では表示されないため)。代わりに、単元内に該当ルールが1件でもあれば`<div class="hint-note">※ の項目は自動検証ができないため、「定着(◎)」には到達しません。紙のドリルでの仕上げが目安です。</div>`をルール一覧の下に常時表示するよう変更(`patternListHtml()`末尾の常時表示注記と同じ考え方)。`openHeatDetail()`側は元々`title`属性ではなく常時表示の`hint-note`だったため確認のみで変更なし。
  - 【苦手トグルの視覚分離】`patternListHtml()`の「⚠️苦手」ボタン(`.mini`、削除ボタンと同じ見た目)を廃止し、パターン名の直前に新設した`.patwarn`(枠なし・アイコンのみ、OFF時は`grayscale(1)`+半透明でくすませ、ON時のみ元の色でくっきり見せる)に置き換え。削除ボタン(`.mini`の枠付きピル)とは形・色ともにはっきり区別されるようにした。
  - 【パターン名の編集】`patternListHtml()`の各行のパターン名を、固定テキスト(`<b>`)から編集可能な`<input type="text">`(`onchange`で確定、既存のテスト範囲一覧の編集パターンと同じ型)に変更。新設した`renamePattern(id,sub,idx,newName)`は名前だけを書き換え、`stage`/`warn`には触れない。空文字への変更は拒否し元の値を保つ(既存の`updateExamRange()`のバリデーション拒否パターンを踏襲)。
  - Node.js上のシミュレーションに、ルールベース単元の説明文・常時表示注記(title属性が残っていないことの確認込み)、`.patwarn`クラスへの置き換え(旧「⚠️苦手」ボタンが無くなっていること)、パターン名編集後も段階・苦手フラグが保持されモーダルに即座に反映されること、空文字への改名が拒否されることを検証するテストを追加して確認ずみ。
- 2026-09-26：テスト勉強モードに「見込みペース表示」を追加。テスト範囲の全単元(ルール7単元＋手動パターン68単元)のうち何割に一度でも触れたかと、直近の消化ペースから「このペースだと一周が終わるのはいつ頃か」を見込み表示し、設定した目標日とのズレを警告する。
  - 新state `S.examDoneLog`(`S.examPlan`とは別のトップレベルの日付辞書、`{日付:その日の消化件数}`)を新設。`markBlock()`の既存exam分岐(`u.done++`の直後)に1行追加して記録する。`rebuildExamPlan()`はunitsを差し替えるだけで`S.examDoneLog`には触れず、`startExamMode()`(真っさらな新規開始)のときだけ`{}`にリセットする。`S.examPlan`の中に入れず外に置いたのは、2026-09-14の「新フィールドの移行漏れ」不具合の教訓を踏まえ、`rebuildExamPlan()`が丸ごとオブジェクトを差し替えてもcarry-over処理を書き忘れるリスクを構造的に無くすため。
  - `examPaceForecast()`を新設。直近7日(`S.examPlan.createdAt`からまだ7日経っていなければ実際の経過日数、最低1日)の1日あたり平均消化件数から、未着手(done=0)単元数÷平均ペースで見込み完了日を算出する。`buildExamPlanUnits()`/`pickExamUnitsForToday()`は無改造(依頼通り、警告はあくまで情報提示のみで計画の割り振りには介入しない)。
  - 「一周完了の目標日」は絶対日付ではなく`S.examPlan.roundGoalDays`(既定7、`setRoundGoalDays()`で変更可)という「一番近い登録テスト日の何日前か」という日数で保持(`rebuildExamPlan()`でも引き継ぐ)。テスト日程を後から変えても自動的に再計算される。
  - `examModePaceHtml()`が状況に応じて「データ不足」「✅ 全単元一周ずみ」「✅ 順調なペースです」「⚠️ ◯日遅れそうです」を出し分け、`examModeCardHtml()`(学習マップ・今日のメニュー双方に既存の仕組みで表示)に追記。
  - 残り時間の目安は、既存の「誤答の登録」画面にある慣習(所要時間の目安は1ページ15分)を「1回=15分」として転用し(`EXAM_MINUTES_PER_REP`)、`examModeSummaryHtml()`の各単元名に「(残り目安◯分)」を追記するのみに留めた(単元のページ範囲を掛け合わせる案は、章の規模によって数百分という非現実的な数字になるため不採用)。
  - **実装中に既存の不具合を発見・修正**：`addDays(iso,n)`が、`toISOString()`(UTC基準)で日付文字列を切り出していたため、日本時間(UTC+9)環境では常に実際より1日早い日付を返していた(例:`addDays("2026-09-19",7)`が本来の9/26ではなく9/25を返す)。ペース計算のローリング平均が合わないことで発覚。`today()`と同じくローカルの`getFullYear/getMonth/getDate`で組み立てる方式に修正。`addDays()`は復習テストの「7日後」期限設定・連続記録(ストリーク)の計算・「今日のメニュー」の直近日除外・週次レビューの表示期間など、本機能と無関係な既存機能でも広く使われており、これらは日本時間環境ではこれまでずっと1日ずれた状態で動いていたことになる。影響範囲が広いため、Nachiに実装方針を確認したうえで、今回まとめて修正した。
  - Node.js上のシミュレーションで、`examDoneLog`の記録・ローリング平均(データが薄い場合のフォールバック含む)・見込み日と目標日の比較による順調/警告の出し分け・`roundGoalDays`の変更と`rebuildExamPlan()`をまたいだ引き継ぎ・`startExamMode()`でのリセット・残り目安分の表示を検証。`addDays()`修正の前後で`addDays(today(),7)`等の計算結果が実際に正しくなったこと(修正前は1日ずれ、修正後は`daysBetween`で検算して一致)も別途確認ずみ(実機ブラウザでの目視確認は未実施、Nachiに依頼)。
- 2026-09-26(続き)：「今日のメニュー」のテスト勉強モード表示の分かりやすさを改修(Nachiからのフィードバック:上部カードの「残りタスク」が全体の残りであることと、下のタイムラインが実際に今日やる一部だけであることの範囲の違いが伝わらず、「今日何個やればいいか分からない」混乱があったため)。ロジック(`examPlan`/`pickExamUnitsForToday()`等)は無改造、表示・文言のみの変更。
  - 見出しを「📋 テスト勉強モード中」→「📋 テスト勉強モード中(全体の残り)」に変更(`renderMenu()`内)。
  - `examModeSummaryHtml(highlightToday)`に引数を追加。`true`を渡すと`pickExamUnitsForToday()`の結果(今日のタイムラインに実際に出る単元)と照合し、一致する単元にだけ`<span class="examtoday">→今日やる</span>`を付ける。「今日のメニュー」からは`examModeSummaryHtml(true)`で呼び、学習マップの`examModeCardHtml()`からは引数なし(従来通りマークなし)で呼ぶため、学習マップ側の見た目は変わらない。
  - カードの説明文に「実際に取り組んだら、下の該当ブロックの『おわった!』を押してください。」を追加し、どこで消化を記録するかを明示。
  - Node.js上のシミュレーションで、今日のpicks(レベル最低の上位2件)にだけ「→今日やる」が付き、それ以外の残りタスクには付かないこと、`highlightToday`を渡さない場合(学習マップ側)は一切マークが付かず既存の見た目のままであることを確認ずみ。
- 2026-09-27：新機能「学習ダッシュボード」を追加。学習時間の見える化による達成感の可視化と、「数学ばかりで国語が手薄」のような教科間の偏りへの気づきを目的とする、表示専用の新画面(サイドメニュー「記録・ごほうび」グループ、テスト記録の下)。
  - まなびばは紙の教材中心で実測の学習時間が無いため、既存の記録に既存の目安時間を掛け合わせた「推定時間」として扱う(画面上部に実測ではなく見積もりである旨を常時明記)。新しい時間基準は作らず、このアプリに既にある「1ページ15分」(誤答の登録画面)・「暗記カード1セッション15分」(今日のメニューのすきま時間ラベル)の2つだけを流用。
  - 紙のドリル(誤答登録・まとめて「できた」を記録の両方が書き込む`S.paperLog`)は`(date,sub,where)`の組でグループ化し、1グループ=1ページ相当=15分とする(`paperMinutesByDay()`)。1問ごとに15分を掛けると、同じページで間違いが多いほど時間が水増しされて実態と乖離するため、この粒度にした。暗記カード(`S.cardLog`)は`(date,sub)`の組が1つでもあれば15分とする(`cardMinutesByDay()`、デッキ名から`S.decks`を引いて教科を特定。デッキが見つからない=削除・改名済みの記録は無視)。両者を合算する`dashMinutesByDay()`/`dashMinutesBySubject()`を新設。
  - 「一言のAIインサイト」はルールベースで実装(`dashboardInsight()`)し、Claude APIは呼ばない。理由：単純な教科間シェアの比較であり自然言語理解を要しない、`S.settings.apiKey`未設定でもダッシュボードの主要な価値(グラフ)が使えるべき、呼び出しごとの課金・レイテンシに見合わない、の3点。突出して多い教科があり触れていない教科がある場合に名指しで促す/全体的にバランスが良ければ肯定する/記録が無ければ案内する、の3パターンを出し分ける。
  - 円グラフ(`pieChartSVG()`)・横棒グラフ(`barChartSVG()`)・積み上げ棒グラフ(`stackedBarChartSVG()`)を新設。外部チャートライブラリは使わず、既存の`lineChartSVG()`(テスト記録画面の点数推移グラフ)と同じ「自前でinline SVGを組み立てる」流儀をそのまま踏襲。積み上げ棒グラフは教科ごとのチェックボックスで表示/非表示を切り替えられ、状態は画面用の一時変数`dashVisibleSubs`(保存しない・画面を開くたびに全表示に戻る)で持つ。
  - 期間指定は`S.longBreakRange`と同じ`<input type=date>`2つのUIパターンを流用し、「直近7日」「今週」「今月」「テスト期間」(`S.examPlan.createdAt`〜直近の登録テスト日。`S.examPlan`が無ければボタン自体を隠す)のクイック選択ボタンを併設。
  - 教科の色は`SUBJECTS`の実際の教科ID(math/eng/sci/geo/his/jpn)にそれぞれ対応する新しい`DASH_SUBCOLOR`を新設(既存の`renderTestCharts()`内のローカルな`SUBCOLOR`は通知表科目の「社会」を1つにまとめた5科目用で、地理・歴史を分けて扱うこのアプリの実際のデータ粒度とは噛み合わないため、あえて共有・流用はしなかった)。
  - 誤答登録・暗記カード・テスト勉強モードなど、データを生成する側の既存ロジックは無改造(読み取り専用の集計・表示レイヤーとして追加)。新しい永続stateも無し。
  - Node.js上のシミュレーションで、ページ/セッション単位のグループ化(同じページの複数誤答が二重計上されないこと・デッキ不明な記録が無視されること)、教科別合算、インサイトの3パターンの出し分け、3種のチャートが0件データでも例外を起こさないこと、クイック期間ボタン(直近7日・テスト期間)の日付セット、チェックボックスの切り替えを確認ずみ(実機ブラウザでの目視確認は未実施、Nachiに依頼)。
- 2026-09-27(続き)：`S.examDoneLog`(テスト勉強モードの日別消化件数)を教科の区別が無い形式から教科別に変更し、学習ダッシュボードに統合(Nachiからの指摘:テスト勉強モード中の学習こそ教科の偏りを一番知りたい場面のはずで、ダッシュボードから除外していたのは本末転倒だったため)。
  - データ構造を`{日付:件数}`から`{日付:{教科:件数}}`に変更。書き込み側は`markBlock()`のexam分岐(該当単元`u`が元々`u.sub`を持っているため、そのまま使えるだけで済んだ)、読み込み側は`examPaceForecast()`のローリング平均集計の1行(`Object.values(...).reduce(...)`で合計する形に変更)のみで、`examModePaceHtml()`本体・`buildExamPlanUnits()`・`pickExamUnitsForToday()`・`startExamMode()`・`rebuildExamPlan()`は無改造。「全体件数=教科別内訳の合計」のため、ペース計算の結果は変更前後で完全に一致する(同じテストケースで検証ずみ)。
  - `load()`に移行処理を追加し、直近実装したばかりのため万一残っている旧形式(値が数値)のデータを`{_unknown:数値}`に変換(合計値は保たれるためペース計算は連続、教科が分からない記録としてダッシュボードの教科別集計からは除外)。
  - 学習ダッシュボード側に`examModeMinutesByDay()`を新設し、`S.examDoneLog`の教科別件数×`EXAM_MINUTES_PER_REP`(15分、既存の定数)を紙のドリル・暗記カードと同じ形の日別・教科別推定時間として算出。`dashMinutesByDay()`(既存の合算関数)に3つ目のソースとして追加。`_unknown`キーは集計から除外し無視する。3ソースが同一の学習セッションを指していて重複計上になりうる点は、ダッシュボードがもともと「実測ではなく見積もり」と明記している性質上許容する設計判断。
  - Node.js上のシミュレーションで、教科別書き込み(`markBlock()`経由)、ペース計算の合計値が変更前と同じ結果になること、旧形式データの`_unknown`退避、`examModeMinutesByDay()`が`_unknown`を除外しつつ正しく分に変換すること、紙のドリル・暗記カード・テスト勉強モードの3ソースが教科ごとに正しく合算され無関係な教科(記録の無いjpn等)に影響しないことを確認ずみ(実機ブラウザでの目視確認は未実施、Nachiに依頼)。
- 2026-09-27(続き2)：「今日の入力」画面を作り替え。教科ごとのページ番号入力(実際にはほとんど使われていなかった)を廃止し、「今日、家庭学習した時間(分)」の入力に置き換えた。スタサプ・やさまる等アプリ外での学習時間を直接記録できるようにし、学習ダッシュボードの精度を上げるのが狙い。
  - 新state `S.homeMinutes`(`S.cardLog`と同じ、日付→教科→分の辞書)を新設。`S.input`は日次リセットされる一時状態のため履歴の入れ物には使えず、別に持たせた。`S.input.entries`は完全に廃止し、`S.input`は`{date,off}`のみに簡素化。
  - `renderInput()`の行は、既存の「タップで開閉」UI(`.subj-row2.on`)をそのまま流用し、中身をページ入力(`{page}`)から分入力に置き換えた。**「未入力」と「0分」を明確に区別**：行が閉じている(オフ)=未入力=学習ダッシュボードは推定値を使う、行が開いている(オン、0分も含む)=実測あり=推定値をその値で上書きする、という設計にした。既存のページ入力は`parseInt()`の真偽値で判定しており"0"を空欄と区別できない作りだったため、同じ罠を踏まないよう明示的に直した。保存は`onchange`(確定時)。
  - **影響範囲の調査(実装前にNachiへ報告ずみ)**：`S.input.entries`に依存する箇所は10箇所のみと判明。うち`renderMenu()`冒頭の`entSubs`は調査の結果**現状すでに未使用の残骸変数**だった。`pickSubjectsForToday()`の「①平日:今日習った教科から」(`kind:"today"`)は、ページ入力の形骸化によりすでに大半の日で発動していなかったと考えられ、廃止後は休日と同じ「苦手ベース」の選定に一本化される(既存の`fill()`を使う長期休み/休日の分岐とは別の、素の`topDebts(6).filter(gen)`によるフォールバックで、これは元々あった経路をそのまま残しただけ)。対策プラン・小テスト予告・すきま時間ローテーション・復習テスト・テスト勉強モードは無改造。
  - 削除：`openUnitPicker()`/`pickUnitPage()`/`closeUnitPick()`と`#unitPickModal`(UNITMAPから単元を選んでページを埋める補助モーダル、存在意義が無くなったため)、`todaySubs()`、`renderMenu()`の「📝 今日の入力がまだです」バナー、`makeMenu()`/`remakeMenu()`の`S.pages`自動更新・巻き戻し処理。`renderCards()`(暗記カード画面)の`ts=todaySubs()`は`ts=[]`に簡略化し、今日の教科優先表示・未登録デッキ警告は自然に出なくなる(呼び出し箇所は6箇所あり、いずれも空配列で安全に動作する形にとどめ、テンプレート文字列の書き換えは行っていない)。
  - **既知の副作用(Nachi確認・了承ずみ)**：`S.pages`(教科書の到達ページ)の自動更新が無くなり、以後は設定画面「📚 進度(前回ページ)の修正」(既存の手動編集UI、`renderSet()`)からの手動更新のみになる。すきま時間の暗記デッキの「まだ習っていない単元は隠す」判定(`isAheadOfProgress()`)がこの手動更新頼みになる。
  - 学習ダッシュボードの`dashMinutesByDay()`を、紙のドリル・暗記カード・テスト勉強モードの推定合算(`dashEstimatedMinutesByDay()`に切り出し)から、「(日付,教科)ごとに`S.homeMinutes`の実測があればそちらを正として使い、無ければ推定にフォールバックする」方式に変更。二重計上を避けるため単純合算はしない。
  - Node.js上のシミュレーションで、`ensureToday()`が旧形式`entries`を持たなくなったこと、行のタップ開閉(0分初期化→削除)・分入力の保存・不正値(空/マイナス)の0への丸め・再描画時の状態復元、`pickSubjectsForToday()`が平日でも苦手ベース2件を返しmasteredなルールを除外すること、ダッシュボードの実測優先ロジック(未入力→推定・0分明示→実測0・値あり→実測優先・実測のみの日も反映)を確認ずみ(実機ブラウザでの目視確認は未実施、Nachiに依頼)。

- 2026-09-26：学習時間を手入力する方式から、別ページの学習タイマーで自動記録する方式へ変更。
  - `timer.html`を追加し、数学・英語・国語・理科・地理・歴史の選択、学習/延長/休憩時間の設定、開始時刻からの復元、休憩の手動再開、予定時間での自動記録を実装。記録・進行中状態は本体の`manabiba3`とは別の`manabibaTimerV1`キーに保存し、日付をまたぐ学習区間は日付ごとに分ける。
  - ダッシュボードではタイマー記録を(日付,教科)ごとの実測として最優先し、記録がない場合は既存の`S.homeMinutes`、それも無ければ従来の推定値を使う。既存の`S.homeMinutes`は削除・変更しない。連続記録は従来の`S.daily`判定に加え、タイマー学習が1日合計10分以上ならその日も学習日とする。
  - 「今日の入力」から分数入力欄を削除してタイマーへの導線に置き換え、学校休み・長期休み・自宅勉オフ日・連続記録・小テスト予告は維持。サイドメニューにも別タブで開く導線を追加し、アプリ起動用の`timer.webmanifest`と`timer-icon.svg`を追加。

- 2026-09-26(続き)：データ管理画面のJSONバックアップ・復元に、別キーで保存する学習タイマーのデータを追加。
  - 書き出しでは`manabibaTimerV1`の内容をトップレベルの`timerData`としてJSONへ含める。復元時は`timerData`があればタイマーキーも更新し、形式不正や保存失敗は通知する。
  - 旧バックアップなど`timerData`を持たないJSONではタイマーキーに触れず、現在の記録を保持する。

## UXの既知課題（2026-09-14時点のレビューより）

今後の改修判断の材料として記録。優先度の高い順。

1. **構造的な問題**：まなびばの機能はほぼ全て「香凜さんが自分から開く・入力する」前提で設計されている。しかし実際には自発的な利用が続かない（類題チャレンジをやらなくなった、「なぜ？」が自分からは生まれない等）。これは個々のUI改善では解決しない、設計思想そのものの限界。
2. **今日の入力**：ページ番号方式は「何を学んだか」という質的な情報を拾えない（例：理科実験の内容が「p.60〜62」に圧縮される）。
3. **誤答登録フォームの使い分けが不透明**：「まとめて『できた』を記録」はタグが付かず対策プランに一切反映されないが、その違いが画面上から伝わらない。
4. **対策プランの解消条件が不透明**：`isTagResolved()`は「同じ(教科,タグ)で一番新しい記録がgreenか」だけを見ているが、ユーザーからはなぜ消えた/消えないのか説明が無く予測できない。
5. **状態変化のフィードバックが静か**：一括取り込みの進捗非表示による誤連打・重複登録（2026-08-31に対処済み）のような、処理中や完了時の視覚的フィードバック不足が繰り返し問題を起こしている。
6. **一貫性の欠如**：ページ番号ベース(UNITMAP)とルールベース(S.rules)という2つの分類軸が長らく別々に走っていた（学習マップで統合されつつある）。

## 開発者について

- 開発者（Nachi）は非エンジニア。技術的な専門用語は避け、平易な言葉で説明すること。
- 出力は日本語、丁寧語。過度な前置きや誇張表現は避け、簡潔に。
