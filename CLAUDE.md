# まなびば プロジェクト概要（Claude Code用ガイド）

このファイルは、まなびば開発における方針・ルール・過去の経緯をまとめたものです。
Claude Codeはこのファイルを読み込んでから作業してください。

## プロジェクトの目的

中学1年生の娘（かりん）の家庭学習を支援するための、単一HTMLファイルの自己完結型Webアプリ。
サーバー不要、ブラウザのみで動作。GitHub Pagesで公開。

デプロイ先: `https://nachi-jazzmusic.github.io/manabiba/manabiba_v3.html`

## 開発の基本哲学（重要）

- アプリは「記録・スケジューリングのためのシステム」として使う。
  実際の勉強そのものは、紙の教材（ドリル・問題集）を使って行う。
  → アプリに問題を全部詰め込む方向ではなく、記録・管理・復習リマインドに徹する設計。
- 「できたつもり」（わかった気になっているだけで実は理解していない状態）を防ぐことを重視。
  そのための仕組みが「ルール型学習」（下記参照）。

## ルール型学習フレームワーク

- ドリル問題に「ルールキー」というタグを付与する。
- 生徒は答えを確認する前に、口頭でそのルールキーに対応する考え方を説明する必要がある。
- 対象教科：数学、英語文法、理科（生物・化学）、国語
- 対象外（暗記のみ）：歴史・地理、漢字
- ルールキーは事前に承認されたリストからのみ使用する（勝手に新しいキーを作らない）。
- カードのタグ表記：`ルール型｜key:キー名`

## フラッシュカードのデータ形式

### 現行フォーマット（line-break-separated形式）

各項目は改行で区切って1行ずつ記載する。

```
問題:
（ここに問題文）
選択肢:
（選択肢。複数ある場合は改行で区切る）
答え:
（正解）
解説:
（解説文）
```

- カード本文中に「｜」（半角パイプ）を使用しない（禁止）。
- 全角パイプ「｜」はタイトル行（デッキ名など）でのみ使用可。
- 旧フォーマット（`問題＝答え`形式、パイプ区切り）は廃止済み。混在させない。

### デッキの命名規則

```
[教科]｜[学年][学期]｜[種類]｜[単元]｜[テスト区分]
```

### その他の制約

- 1デッキあたり最大20問程度に収める。
- 手書きの書き込み（採点済みの丸バツなど）は無視して問題本文のみ抽出する。
- 画像がないと解けない問題には `【絵】` のフラグを付ける。
- べき乗はUnicodeの上付き文字を使う（例：5²）。

## 既知の課題・改善予定

- **英語スペルの自己採点精度の問題**：現状の自己申告方式では、スペルミスをスペルミスと認識できないまま「できた」と自己判定してしまうケースがある。
  → 対策：タイプ入力＋自動文字列マッチング方式に変更する。この変更にはHTML側の修正が必要（未着手）。

## Git操作について

Nachiはターミナルにコマンドを直接入力しない。Git操作はすべて会話での指示で行う。

- 「Pushして」「保存して」などと言われたら、以下を自動で実行する：
  ```
  git add .
  git commit -m "（変更内容がわかる一言メッセージを自動生成）"
  git push origin master
  git push origin master:main
  ```
- commitメッセージは、直前の変更内容をもとに自動で生成してよい（Nachiに聞き返さなくてよい）。
- 「Pushして」と言われるまでは、上記の標準フローの通りcommit・pushは行わない。
- pushの結果（成功/失敗）は必ず一言で報告する。エラーが出た場合は、対処法も含めて分かりやすく説明する。

## Git / GitHub運用ルール

- **作業ブランチは `master`。GitHub Pagesの公開元は `main`。** そのため「Pushして」の際は必ず両方に反映する（上記コマンド参照）。
  - 2026-08-22に発覚：mainが古いまま更新されておらず、masterにだけpushしていたため本番に変更が反映されない事故があった。以後、両ブランチへのpushを標準フローに組み込んでいる。
  - 将来的に運用をシンプルにする場合はmasterに一本化する選択肢もあるが、現時点ではブランチ整理は行わず「両方にpush」で運用する。
- **APIキーなど機密情報を含むファイルは絶対にコミットしない。**
  過去に `bk2/manabiba_backup_2026-08-11.json` にAPIキーが混入し、GitHubのpush protectionでブロックされた経緯がある。
  バックアップファイルをコミットする前に、APIキーが含まれていないか必ず確認すること。

## 実装時の標準フロー

機能追加・修正を依頼されたら、以下の順で進める。

1. 要件を実装する。
2. ブラウザで実際に動かして確認する（可能な場合、Claude in Chromeなどのブラウザ確認手段を使う）。
3. エラーや不具合があれば自分で直し、直るまで2に戻る。
4. 変更内容を、このファイルの「開発ログ」セクションに追記する（日付・変更点・変更理由を簡潔に）。
5. 下記フォーマットで「確認チェックリスト」を出力する。
6. ここで一度止まり、Nachiに完了報告する。**Gitへのcommit・pushは、Nachiの明示的な指示があるまで行わない。**

## 完了報告時のチェックリスト出力フォーマット

実装が完了したら、以下の形式で「Nachiが目視確認すべき項目」を出力する。
一般論ではなく、**今回変更した箇所に紐づいた具体的な確認項目**にすること。

```
## 今回の変更内容
（何をどう変えたかを1〜3行で）

## 確認してほしいこと（Push前チェックリスト）
- [ ] （変更した機能が意図通り動くか。具体的な操作手順つきで）
- [ ] （既存の別機能を壊していないか。特に影響がありそうな箇所）
- [ ] （かりんが実際に使う想定での見え方・押しやすさ）
- [ ] （スマホ/タブレットなど、普段使っている端末での見え方）
- [ ] （エラーが起きた時の見え方・挙動）

## 変更したファイル
（ファイル名一覧）
```

チェックリストの項目数は変更の大きさに応じて増減してよいが、
「動いた」だけでなく「壊していないか」「かりんにとって使いやすいか」の観点は毎回必ず含めること。

## 開発ログ

（実装が完了するたびに、ここに日付・変更内容を追記していく）

- 2026-08-22：CLAUDE.md作成。開発方針・ルール型学習フレームワーク・カード形式・Git運用ルールを整理。
- 2026-08-22：親ポメ(sPPome)の入力欄に、音声入力（🎤）と手書き入力（✍️）ボタンを追加。ポメに聞く(sPome)画面と同じ`voiceInput()`/`openPad()`関数・`.inp-tools`のUIパターンをそのまま流用（新規実装なし）。
- 2026-08-22：サイドメニューの「ポメに聞く」を押した時、常に新規チャット状態で始まるよう修正。`openPomeFresh()`を追加し、直前の会話が未使用の空チャットでなければ新しい空チャットを先頭に追加してから画面を開く。過去の会話データは削除・上書きせず、「これまでの会話」一覧からの再オープンも従来通り。
- 2026-08-22：「ポメに聞く」「親ポメ(相談)」のチャット表示エリアの高さを拡大。`.pome-layout`(ポメに聞く)とsPPomeの`.chat-fixed`の`calc(100vh - 205px/220px)`を、実際のヘッダー高さに合わせて`calc(100vh - 150px)`に統一。下に余っていた余白を減らし、入力欄・送るボタンは従来通り下固定。横幅・サイドメニュー・これまでの会話一覧のレイアウトは変更なし。
- 2026-08-22：親ポメ(sPPome)の会話診断をS.rulesに反映できる機能を追加。
  - PPOME_SYSに「返信の最後の行に`[[DIAG: ルールキー,...]]`を付ける」指示を追加し、送信時に承認済みルールキー(S.rulesのid)一覧を動的に渡す。マーカーが無い/壊れている場合は診断なしとして通常通り表示(フォールバック)。
  - 診断が付いた返信の下に「🔍この診断を反映」ボタンを表示→押すと確認モーダル(診断されたルールを個別にチェックのON/OFFで選べる)を開き、確定したものだけ反映。
  - 反映内容は既存の「再現性不足」ロジックと同じ(status:"mastered"→"none"に戻すだけ。今回新設)。ミスの種類を記録する既存のtagHistとは別の`r.diagHist`(親ポメ診断専用)に記録し、既存データと混在させない。
  - 反映後は「✅診断を反映ずみ」表示に変わり、二重登録を防止。
  - 類題チャレンジ画面(sChal)のルール一覧に、直近の診断で◎が見直されたルールへ「🐕見直し」の印を表示。
  - 注意点：`pickSubjectsForToday()`は変更していないため、`gen`が`null`のルール(理科・社会など一部)は今回の反映をしても「今日のメニュー」には出ない(数学・英語中心で効果あり)。
- 2026-08-24：「今日のメニュー」のすきま時間欄を、曜日固定のローテーション方式に変更。
  - 毎日「暗記カード系(漢字/英単語)」1つ＋「デジタルドリル系(社会メクリカ/国語さくスタ/理科さくスタ/英単語タイピング)」1つだけを表示するよう、`SUKIMA_ROTATION`(曜日→ジャンルの対応表)を新設。既存の`memoLine()`・`extLinkAct()`はそのまま流用。
  - 今日の指定以外もすぐ選べるよう「ほかのジャンルも見る」の`<details>`開閉パネルを追加(一覧は隠さない)。
  - `pickSubjectsForToday()`・`makeMenu()`本体は変更なし(メイン学習2教科の選定ロジックに影響なし)。
  - 「口頭チェックのカンペ」ショートカットは、今日のメニューからは表示削除(結局使われていなかったため)。暗記カード画面(sCards)の「今日やったカード」タブ自体は残したまま(機能自体の削除は見送り)。
- 2026-08-25：すきま時間の暗記カード枠と、小テスト対策の重複を解消。
  - 小テスト登録フォーム(国語/英語選択時のみ)に、対象デッキを複数選べるチェックボックスを追加。`S.quizzes`に任意項目`deckIds`を追加(未設定の既存データはフォールバックで影響なし)。
  - すきま時間の暗記カード枠は、直前期間(登録から3日以内・既存のdaysBetween基準を踏襲)かつ今日のローテーション教科と一致する小テストにデッキが紐付けてあれば、通常のローテーションではなく紐付けデッキを進捗ベース(やりかけ→手つかず→進み具合の少ない順)で最大2件まで「📢小テスト対策」として表示するよう`topMemoDecksFromIds()`を新設。3件以上あっても、終わった分から自動的に次のデッキに繰り上がる(日数カウント等の新しい状態は持たせていない)。
  - 紐付けデッキが全部終わったら「✅ 小テスト対策はここまで完了ずみ」を表示。
  - 登録済み小テスト一覧にも、紐付けたデッキ名を追記表示。
  - `startMemo()`の共通処理を`openDeckAndStart()`に切り出し、デッキIDを直接指定して開ける`startMemoDeck()`を追加(小テスト対策デッキを開く用)。
  - `SUKIMA_ROTATION`本体・`pickSubjectsForToday()`・`makeMenu()`本体は変更なし。
- 2026-08-25：不具合修正。小テスト登録のデッキ選択チェックボックスに、登録済みデッキの一部しか出ていなかった。
  - 原因：`updateQuizDeckPick()`が、自動ローテーション用にデッキ名を「漢字/熟語/語彙」等のキーワードで絞り込む`memoDecks()`を流用していたため、キーワードに一致しない命名のデッキ(読解単元と一体化したデッキ等)が候補から漏れていた。
  - 修正：手動選択の用途に合わせ、絞り込みなしでその教科の登録デッキを全部候補に出すよう変更(`S.decks.filter(d=>d.sub===sub)`)。
- 2026-08-25：不具合修正。復習テスト(類題チャレンジ→7日後の復習)で、正解しても問題がリストから消えないことがあった。
  - 原因：`renderChal()`が、復習テスト一覧を更新する`renderRev()`を関数の一番最後に呼ぶ作りだったため、途中(ルール一覧の描画)で処理が止まると`renderRev()`が実行されず、一覧が更新されないままになっていた。再読み込みすると直っていたのは、データ(正誤の記録)自体は正しく保存されていたため。
  - 直前の小テストのデッキ選択修正は無関係と確認ずみ。ただし、その少し前に追加した親ポメ診断の🐕マーク表示処理が、この構造上の弱さを顕在化させた可能性がある。
  - 修正：`renderChal()`内で`renderRev()`を関数の一番最初に呼ぶよう順序を変更。以後、ルール一覧側で何が起きても復習テスト一覧の更新は必ず行われる。あわせて`diagHist`参照部分をより防御的な書き方に修正。

## 開発者について

- 開発者（Nachi）は非エンジニア。技術的な専門用語は避け、平易な言葉で説明すること。
- 出力は日本語、丁寧語。過度な前置きや誇張表現は避け、簡潔に。
